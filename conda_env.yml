name: adaptive
channels:
  - conda-forge
  - defaults
dependencies:
  - anaconda
  - docker-py
  - pip:
    - firebase-admin
    - openai
    - pyautogen
    - transitions
prefix: /opt/conda/envs/adaptive